#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Optimize ALL images (idempotent — skips up-to-date ones)
node scripts/optimize-images.mjs

# Stage new/changed/deleted web-optimized images so they're part of this commit
# Pathspecs MUST be quoted so Git (not /bin/sh) does the ** globbing
git add -A 'src/assets/origami/**/web' 'src/assets/projects/**/web' 2>/dev/null || true
